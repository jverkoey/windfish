syntax = "proto3";

message ROM {
  repeated ExecutionRegion executionRegion = 1;
  repeated Labels label = 2;
}

message ExecutionRegion {
  uint32 startAddress = 1;
  uint32 endAddress = 2;
  uint32 bank = 3;
}

message Labels {
  uint32 address = 1;
  uint32 bank = 2;
  string name = 3;
}
